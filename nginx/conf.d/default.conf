upstream uz_checkpoint {
    server web:8000;
}

server {
    listen 80;
    server_name yikkan.com www.yikkan.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name yikkan.com www.yikkan.com;

    ssl_certificate /etc/letsencrypt/live/yikkan.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/yikkan.com/privkey.pem;

    location / {
        proxy_pass http://uz_checkpoint;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header Host $host;
        proxy_redirect off;
    }

    location /static/ {
        alias /app/static/;
    }

    location /media/ {
        alias /app/media/;
    }
}
